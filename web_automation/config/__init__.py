# This __init__.py file is intentionally left empty or can be used
# for package-level docstrings or other initializations that do not
# involve importing from sibling modules like settings.py at load time
# to prevent circular dependencies.
#
# Consumers should import specific config objects directly from
# web_automation.config.settings (e.g., from web_automation.config.settings import general_config)
